@page 
@model Korepetycje_Matematyka.Pages.TerminyModel
@{
}
<h1>Dostępne terminy korepetycji</h1><hr /><hr />
<div class="table-responsive">
<table class="table table-striped table-bordered">
    <thead>
        <tr>
           <th><center>Dzień Tygodnia</center></th>
           <th><center>Godzina </center></th>
           <th><center>Godzina </center></th> 
           <th><center>Godzina </center></th>
           <th><center>Godzina </center></th>
           <th><center>Godzina </center></th>  
           <th><center>Godzina </center></th>
           <th><center>Godzina </center></th>
           <th><center>Godzina </center></th>
           <th><center>Godzina </center></th>
           <th><center>Godzina </center></th>
        </tr>
    </thead>
    <tbody>
         @foreach(var item in Model.Terminy)
        {
                <tr>
           <form method="post"> <td> <center><button class="btn btn-primary mt-2"  asp-route-day="" asp-route-value="@item.Dzień_Tygodnia">@item.Dzień_Tygodnia</button><br /></center></td></form>
           <form method="post"> <td> <center>@if(item.Godzina != "Niedostępny"){<button class="btn btn-success" asp-route-column="@nameof(@item.Godzina)" asp-route-id="@item.Id" asp-route-day="@item.Dzień_Tygodnia" asp-route-value="@item.Godzina">@item.Godzina</button>}</center></td></form>
           <form method="post"> <td> <center>@if(item.Godzina1 != "Niedostępny"){<button class="btn btn-success" asp-route-column="@nameof(@item.Godzina1)" asp-route-id="@item.Id"  asp-route-day="@item.Dzień_Tygodnia" asp-route-value="@item.Godzina1">@item.Godzina1</button>}</center></td></form>
           <form method="post"> <td> <center>@if(item.Godzina2 != "Niedostępny"){<button class="btn btn-success" asp-route-column="@nameof(@item.Godzina2)" asp-route-id="@item.Id"  asp-route-day="@item.Dzień_Tygodnia" asp-route-value="@item.Godzina2">@item.Godzina2</button>}</center></td></form>
           <form method="post"> <td> <center>@if(item.Godzina3 != "Niedostępny"){<button class="btn btn-success" asp-route-column="@nameof(@item.Godzina3)" asp-route-id="@item.Id"  asp-route-day="@item.Dzień_Tygodnia" asp-route-value="@item.Godzina3">@item.Godzina3</button>}</center></td></form>
           <form method="post"> <td> <center>@if(item.Godzina4 != "Niedostępny"){<button class="btn btn-success" asp-route-column="@nameof(@item.Godzina4)" asp-route-id="@item.Id"   asp-route-day="@item.Dzień_Tygodnia" asp-route-value="@item.Godzina4">@item.Godzina4</button>}</center></td></form>
           <form method="post"> <td> <center>@if(item.Godzina5 != "Niedostępny"){<button class="btn btn-success" asp-route-column="@nameof(@item.Godzina5)" asp-route-id="@item.Id"  asp-route-day="@item.Dzień_Tygodnia" asp-route-value="@item.Godzina5">@item.Godzina5</button>}</center></td></form>
           <form method="post"> <td> <center>@if(item.Godzina6 != "Niedostępny"){<button class="btn btn-success" asp-route-column="@nameof(@item.Godzina6)" asp-route-id="@item.Id"  asp-route-day="@item.Dzień_Tygodnia" asp-route-value="@item.Godzina6">@item.Godzina6</button>}</center></td></form>
           <form method="post"> <td> <center>@if(item.Godzina7 != "Niedostępny"){<button class="btn btn-success" asp-route-column="@nameof(@item.Godzina7)" asp-route-id="@item.Id"  asp-route-day="@item.Dzień_Tygodnia" asp-route-value="@item.Godzina7">@item.Godzina7</button>}</center></td></form>
           <form method="post"> <td> <center>@if(item.Godzina8 != "Niedostępny"){<button class="btn btn-success" asp-route-column="@nameof(@item.Godzina8)" asp-route-id="@item.Id"  asp-route-day="@item.Dzień_Tygodnia" asp-route-value="@item.Godzina8">@item.Godzina8</button>}</center></td></form>
           <form method="post"> <td> <center>@if(item.Godzina9 != "Niedostępny"){<button class="btn btn-success" asp-route-column="@nameof(@item.Godzina9)" asp-route-id="@item.Id"  asp-route-day="@item.Dzień_Tygodnia" asp-route-value="@item.Godzina9">@item.Godzina9</button>}</center></td></form>
            </tr>
        }
    </tbody>
    </table>
</>
